export type Movie = {
    id: string
    title: string
    year: string
    image: string
}

export type MovieResponse = {
    Title: string;
    Year: string;
    imdbID: string;
    Type: string;
    Poster: string;
}